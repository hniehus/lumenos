# Appendix B — Immediate Follow-Up Work

- freeze the kernel type explicitly
- turn the Kernel Charter into a strict two-column contract
- define lifecycle rules for each kernel object
- specify the boot contract payload precisely
- specify the exact boot-to-init handoff representation
- define the first synchronous IPC message model
- define the exact init responsibilities for the first user-space bootstrap
